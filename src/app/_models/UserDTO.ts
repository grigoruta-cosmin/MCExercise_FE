import { PhotoDTO } from "./PhotoDTO";

export interface UserDTO {
    firstName: string;
    lastName: string;
    email: string;
    country: string;
    bio: string;
    usermame: string;
    type: string;
}